# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # The maxInstances setting has been removed to allow for automatic scaling on Vercel.
  # For Firebase App Hosting, you can re-add this with a higher value if needed.
  # For example:
  # maxInstances: 10
[]